services:
  - type: web
    name: crypto-research-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python crypto_research_bot_final.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
